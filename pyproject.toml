[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# E/W: pycodestyle, F: pyflakes, I: import sorting.
select = ["E", "F", "W", "I"]
# Long feed URLs in feeds.py intentionally exceed the line length.
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
